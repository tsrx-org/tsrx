---
'@tsrx/mcp': patch
---

`review_tsrx_styles` recognizes self-closing `<style apply={…} />` blocks and reports exported or applied blocks as themes, and the documentation index covers the scoped-style sections of the specification.
