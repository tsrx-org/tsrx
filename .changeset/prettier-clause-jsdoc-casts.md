---
'@tsrx/prettier-plugin': patch
---

An end-of-line JSDoc type cast after an `if`, `for`, `while`, or `do` header stays on the header's line when a non-block body moves to the next line, so the statement keeps the JSDoc type it had.
