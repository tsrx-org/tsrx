---
'@tsrx/typescript-plugin': patch
---

Update the optional `@tsrx/ripple` compiler peer dependency to `^0.2.0`, aligning with [Ripple's release PR #1456](https://github.com/Ripple-TS/ripple/pull/1456). The workspace also updates `ripple`, `@ripple-ts/vite-plugin`, and `@ripple-ts/adapter-node` to `^0.4.0`.
